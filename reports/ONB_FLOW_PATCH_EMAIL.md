# ONB flow patch — Email step inserted, defaults & copy corrected

- **ONB-02 Email** added (3 frames): default (envelope hero tile + glow, "Next, your email", no-spam helper) → filled (`priya.sharma@gmail.com` + processing chip "Verification link sent · tap it whenever") → success ("Inbox, connected" copper saved panel, EMAIL/VERIFICATION rows, "Saved" chip).
- Frames renumbered: PAN + KYC → ONB-03, Residency + FATCA → ONB-04, Bank → ONB-05; canvas re-laid on a clean 475px grid with module gaps. Rail captions already matched (2/3/4/5 of 7); Email rail = 2 segments filled.
- Default-state honesty: mobile placeholder → `+91 · 10-digit mobile`; PAN placeholder → `10-character PAN`; bank IFSC placeholder → `11-character IFSC` (no pre-filled values on defaults).
- Safer copy: "Residency recorded. Bank next." · "No TIN provided" (field) / "Not provided" (row) · "A small bank check happens at activation."
- No new states; approved visual language inherited untouched (wash, hero tile, single glow, single metal CTA, quiet rail).
